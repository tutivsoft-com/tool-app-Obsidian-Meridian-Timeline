# Release attestation

Meridian Timeline release artifacts are built from the repository source by the GitHub Actions workflow in `.github/workflows/release-attestation.yml`. The workflow records the commit, version, and SHA-256 digests for the uploadable files under `publish/`.

