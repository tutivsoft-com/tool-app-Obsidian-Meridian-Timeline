# Meridian public mirror analysis

This mirror reflects the validated Meridian surface. Timeline defaults render a first view immediately; account-scoped usage uses authenticated entitlements/spend and durable pending events. Filters and navigation lead the UI, with saved views and advanced rules later. Diagnostics omit vault text. Private-source checks precede synchronization.
